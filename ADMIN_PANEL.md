# ТЗ: Админ-панель для 4roads.su

## Общая информация

**Цель:** Реализовать удобную админ-панель для управления контентом сайта 4roads.su

**Технологии:**
- FastAPI + SQLAdmin
- База данных: PostgreSQL
- Аутентификация: JWT (уже реализована)

**Срок выполнения:** ~8-12 часов

---

## Требования к админ-панели

### 1. Общие требования

- ✅ Использовать библиотеку `starlette-admin`
- ✅ Интеграция с существующей JWT аутентификацией
- ✅ Русский язык интерфейса
- ✅ Поиск и фильтрация по основным полям
- ✅ Пагинация (20-50 записей на страницу)
- ✅ Валидация данных перед сохранением
- ✅ Удобное редактирование связанных сущностей
- ✅ Различные bulk операции например массовая скидка/удаление товаров и т.д

---

## Сущности для управления

### 2.1. Категории (`Category`)

**Модель:** `app.infrastructure.database.models.category.Category`

**Поля:**
- `id` (UUID, auto) - не редактируется
- `name` (str) ⚠️ обязательное, уникальное
- `slug` (str) ⚠️ обязательное, уникальное, генерируется автоматически
- `description` (str, optional)
- `image` (str, optional) - путь к изображению для плитки категории
- `order` (int, default=0) - порядок отображения
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ CRUD операции
- ✅ Поиск по `name`
- ✅ Сортировка по `order`, `name`
- ✅ Массовое изменение `order`
- ✅ Отображение количества товаров в категории
- ✅ Загрузка изображения через админку

**Особенности:**
- `slug` генерируется автоматически из `name` (транслитерация) в service layer
- При удалении категории все товары в ней тоже удаляются (CASCADE)

---

### 2.2. Товары (`Product`)

**Модель:** `app.infrastructure.database.models.product.Product`

**Поля:**
- `id` (UUID, auto)
- `name` (str) ⚠️ обязательное
- `slug` (str) ⚠️ обязательное, уникальное, генерируется автоматически
- `description` (str, optional)
- `price` (int) ⚠️ обязательное - цена в рублях
- `old_price` (int, optional) - старая цена для отображения скидки
- `is_active` (bool, default=True) - видимость на сайте
- `is_featured` (bool, default=False) - отображение в "Рекомендуемых"
- `category_id` (UUID) ⚠️ обязательное - связь с категорией
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Связи:**
- `category` → Category (Many-to-One)
- `images` → ProductImage (One-to-Many)
- `characteristics` → ProductCharacteristic (One-to-Many)
- `reviews` → Review (One-to-Many)

**Функционал:**
- ✅ CRUD операции
- ✅ Поиск по `name`, `description`
- ✅ Фильтр по `category`, `is_active`, `is_featured`
- ✅ Сортировка по `price`, `name`, `created_at`
- ✅ Массовое изменение `is_active`, `category`
- ✅ Inline редактирование `images` и `characteristics`
- ✅ Отображение количества отзывов

**Особенности:**
- Цена хранится как целое число (рубли)
- При удалении товара удаляются все связанные изображения, характеристики, отзывы (CASCADE)

---

### 2.3. Изображения товаров (`ProductImage`)

**Модель:** `app.infrastructure.database.models.product.ProductImage`

**Поля:**
- `id` (UUID, auto)
- `image_path` (str) ⚠️ обязательное
- `order` (int, default=0) - порядок отображения в галерее
- `product_id` (UUID) ⚠️ обязательное
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ Inline редактирование внутри товара
- ✅ Загрузка изображений (автоматическая конвертация в WebP)
- ✅ Изменение порядка отображения
- ✅ Превью изображения в списке

**Особенности:**
- Изображения автоматически конвертируются в WebP (реализовано в `ImageService`)

---

### 2.4. Типы характеристик (`CharacteristicType`)

**Модель:** `app.infrastructure.database.models.product.CharacteristicType`

**Поля:**
- `id` (UUID, auto)
- `name` (CharacteristicTypeEnum) ⚠️ обязательное, уникальное
- `slug` (str) ⚠️ обязательное, уникальное
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Значения Enum:**
- `MATERIAL` - Материал
- `DIMENSIONS` - Размеры
- `WEIGHT` - Вес
- `COLOR` - Цвет
- `MANUFACTURER` - Производитель

**Функционал:**
- ✅ Просмотр списка
- ❌ НЕЛЬЗЯ добавлять новые типы (фиксированный Enum)
- ❌ НЕЛЬЗЯ редактировать
- ❌ НЕЛЬЗЯ удалять

**Особенности:**
- Эти типы создаются при инициализации БД (seed data)
- Используются для стандартизации характеристик товаров
- Набор фиксированный - только те, что в Enum

---

### 2.5. Характеристики товаров (`ProductCharacteristic`)

**Модель:** `app.infrastructure.database.models.product.ProductCharacteristic`

**Поля:**
- `id` (UUID, auto)
- `value` (str) ⚠️ обязательное
- `product_id` (UUID) ⚠️ обязательное
- `characteristic_type_id` (UUID) ⚠️ обязательное
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ Inline редактирование внутри товара
- ✅ Выбор типа характеристики из выпадающего списка
- ✅ Ввод значения

**Пример:**
- Тип: "Материал", Значение: "Алюминий"
- Тип: "Размеры", Значение: "120x80x60 см"

---

### 2.6. Отзывы (`Review`)

**Модель:** `app.infrastructure.database.models.review.Review`

**Поля:**
- `id` (UUID, auto)
- `author_name` (str) ⚠️ обязательное
- `content` (str) ⚠️ обязательное
- `rating` (int) ⚠️ обязательное - оценка от 1 до 5
- `image` (str, optional) - фото к отзыву
- `is_active` (bool, default=True) - видимость на сайте
- `product_id` (UUID) ⚠️ обязательное
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ CRUD операции
- ✅ Фильтр по `is_active`, `rating`, `product`
- ✅ Поиск по `author_name`, `content`
- ✅ Сортировка по `rating`, `created_at`
- ✅ Массовое изменение `is_active`
- ✅ Загрузка изображения к отзыву
- ✅ Inline редактирование внутри товара

**Особенности:**
- Админ создаёт отзывы вручную (нет публичной формы)
- Валидация: `rating` от 1 до 5

---

### 2.7. FAQ (`FAQ`)

**Модель:** `app.infrastructure.database.models.faq.FAQ`

**Поля:**
- `id` (UUID, auto)
- `question` (str) ⚠️ обязательное
- `answer` (str) ⚠️ обязательное
- `is_active` (bool, default=True) - видимость на сайте
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ CRUD операции
- ✅ Поиск по `question`, `answer`
- ✅ Фильтр по `is_active`
- ✅ Сортировка по `created_at`
- ✅ Массовое изменение `is_active`

---

### 2.8. Настройки сайта (`Settings`)

**Модель:** `app.infrastructure.database.models.settings.Settings`

**Поля:**
- `id` (int, auto)
- `phone` (str, optional)
- `email` (str, optional)
- `address` (str, optional)
- `work_hours` (JSON, optional) - структура:
  ```json
  {
    "weekdays": {"start": "09:00", "end": "18:00"},
    "weekend": {"start": "10:00", "end": "16:00"},
    "note": "Заказы на сайте принимаются 24/7"
  }
  ```
- `vk_url` (str, optional)
- `telegram_url` (str, optional)
- `whatsapp_url` (str, optional)
- `youtube_url` (str, optional)
- `about_text` (str, optional) - текст "О компании"

**Функционал:**
- ✅ Редактирование единственной записи
- ✅ JSON редактор для `work_hours`
- ❌ Нельзя удалять
- ❌ Нельзя создавать новые записи (только одна запись в БД)

**Особенности:**
- Всегда должна быть ровно одна запись в таблице
- При первом запуске создаётся запись с `id=1` (seed data)

---

### 2.9. Заявки с формы (`ContactForm`)

**Модель:** `app.infrastructure.database.models.contact_form.ContactForm`

**Поля:**
- `id` (UUID, auto)
- `name` (str) ⚠️ обязательное
- `phone` (str) ⚠️ обязательное
- `message` (str) ⚠️ обязательное
- `is_processed` (bool, default=False) - обработана ли заявка
- `created_at` (datetime, auto) - из Base
- `updated_at` (datetime, auto) - из Base

**Функционал:**
- ✅ Просмотр заявок
- ✅ Фильтр по `is_processed`
- ✅ Сортировка по `created_at` (новые сверху)
- ✅ Массовая пометка как "Обработано"
- ❌ Нельзя редактировать данные заявки
- ❌ Нельзя создавать вручную (только через API с фронтенда)

**Особенности:**
- Заявки приходят с фронтенда через API
- Админ только просматривает и отмечает как обработанные

---

## 3. Технические требования

### 3.1. Установка и настройка

```bash
# Установить sqladmin
pip install sqladmin

# Добавить в requirements.txt
sqladmin==0.16.0
```

### 3.2. Интеграция с FastAPI

**Файл:** `app/admin/admin.py`

```python
from sqladmin import Admin, ModelView
from app.main import app
from app.infrastructure.database.session import engine

# Инициализация админки
admin = Admin(app, engine, title="4roads Admin")

# Регистрация моделей
admin.add_view(CategoryAdmin)
admin.add_view(ProductAdmin)
# ... и т.д.
```

### 3.3. Аутентификация

**Важно:** SQLAdmin имеет встроенную систему аутентификации!

**Требования:**
- Использовать встроенный `AuthenticationBackend` от SQLAdmin
- Интегрировать с существующим `AuthService` из `app/core/services/auth_service.py`
- При логине проверять через `auth_service.login_user()`
- Сохранять JWT токен в сессии/cookies
- При каждом запросе проверять токен через `auth_service.verify_token()`

**⚠️ ВАЖНО - ПРОВЕРИТЬ ПЕРЕД РЕАЛИЗАЦИЕЙ:**
1. **Изучи существующие роуты:** `app/api/v1/routers/admin/auth_router.py`
   - Есть ли там `/login` и `/refresh`?
   - Можно ли их использовать для SQLAdmin?
   
2. **Проверь совместимость:**
   - SQLAdmin использует form-based аутентификацию (username/password через HTML форму)
   - Текущие роуты FastAPI работают с JSON
   - **Вывод:** Скорее всего текущие роуты НЕ подходят

3. **Если текущие роуты не подходят:**
   - Создай отдельный `AuthenticationBackend` для SQLAdmin
   - Используй `AuthService` внутри backend'а напрямую
   - Не создавай новые HTTP роуты - SQLAdmin предоставляет свой UI логина

**Пример интеграции:**

```python
from sqladmin.authentication import AuthenticationBackend
from starlette.requests import Request
from app.core.services.auth_service import AuthService

class AdminAuth(AuthenticationBackend):
    async def login(self, request: Request) -> bool:
        form = await request.form()
        username = form.get("username")
        password = form.get("password")
        
        # Используем существующий AuthService
        auth_service = AuthService(...)
        try:
            token_data = await auth_service.login_user(username, password)
            request.session.update({"token": token_data.access_token})
            return True
        except:
            return False
    
    async def logout(self, request: Request) -> bool:
        request.session.clear()
        return True
    
    async def authenticate(self, request: Request) -> bool:
        token = request.session.get("token")
        if not token:
            return False
        
        auth_service = AuthService(...)
        try:
            await auth_service.verify_token(token, "access")
            return True
        except:
            return False

admin = Admin(app, engine, authentication_backend=AdminAuth(secret_key=JWT_CONFIG.SECRET_KEY))
```

**Не нужно:**
- ❌ Создавать отдельные HTTP роуты для логина (SQLAdmin предоставляет свой UI)
- ❌ Использовать существующие FastAPI роуты `/api/v1/admin/auth/` напрямую

### 3.4. Локализация

```python
class CategoryAdmin(ModelView, model=Category):
    name = "Категория"
    name_plural = "Категории"
    
    column_labels = {
        "name": "Название",
        "slug": "URL",
        "description": "Описание",
        "image": "Изображение",
        "order": "Порядок"
    }
```

---

## 4. Примеры кастомизации

### 4.1. Пример для Category

```python
from sqladmin import ModelView

class CategoryAdmin(ModelView, model=Category):
    # Мета-информация
    name = "Категория"
    name_plural = "Категории"
    icon = "fa-solid fa-folder"
    
    # Колонки в списке
    column_list = [Category.name, Category.slug, Category.order, "products"]
    
    # Поиск
    column_searchable_list = [Category.name]
    
    # Фильтры
    column_filters = [Category.order]
    
    # Сортировка по умолчанию
    column_default_sort = [(Category.order, False), (Category.name, False)]
    
    # Редактируемые поля
    form_columns = [
        Category.name,
        Category.slug,
        Category.description,
        Category.image,
        Category.order
    ]
    
    # Кастомное отображение
    column_formatters = {
        "products": lambda m, a: len(m.products)
    }
```

### 4.2. Пример для Product с Inline

```python
class ProductAdmin(ModelView, model=Product):
    # ... базовые настройки ...
    
    # Inline редактирование связанных сущностей
    form_ajax_refs = {
        "category": {
            "fields": ("name",),
            "order_by": "name",
        }
    }
    
    # Отображение связей
    column_formatters = {
        "price": lambda m, a: f"{m.price} ₽",
        "old_price": lambda m, a: f"{m.old_price} ₽" if m.old_price else "-"
    }
```

---

## 5. Чек-лист для разработки

### Подготовка
- [ ] Установить `sqladmin`
- [ ] Изучить документацию: https://aminalaee.dev/sqladmin/
- [ ] **Проверить существующие роуты** `app/api/v1/routers/admin/auth_router.py`
  - [ ] Можно ли их использовать для SQLAdmin?
  - [ ] Если нет - планировать создание `AuthenticationBackend`
- [ ] Создать файл `app/admin/admin.py`
- [ ] Создать файл `app/admin/views.py`
- [ ] Создать файл `app/admin/auth.py` (для `AuthenticationBackend`)

### Реализация ModelView для каждой сущности
- [ ] CategoryAdmin
- [ ] ProductAdmin
- [ ] ProductImageAdmin (inline)
- [ ] CharacteristicTypeAdmin
- [ ] ProductCharacteristicAdmin (inline)
- [ ] ReviewAdmin
- [ ] FAQAdmin
- [ ] SettingsAdmin
- [ ] ContactFormAdmin

### Аутентификация
- [ ] Изучить существующие роуты авторизации
- [ ] Создать `AuthenticationBackend` для SQLAdmin
- [ ] Интегрировать с `AuthService`
- [ ] Протестировать логин/логаут
- [ ] Протестировать защиту страниц админки

### Дополнительно
- [ ] Русифицировать интерфейс
- [ ] Добавить кастомные валидаторы (например, для `price > 0`, `rating` 1-5)
- [ ] Настроить загрузку изображений
- [ ] Добавить превью изображений в списке
- [ ] Реализовать seed data для CharacteristicType
- [ ] Реализовать seed data для Settings (создание единственной записи)
- [ ] Протестировать все CRUD операции
- [ ] Протестировать массовые операции
- [ ] Протестировать inline редактирование

---

## 6. Дополнительные материалы

**Документация:**
- SQLAdmin: https://aminalaee.dev/sqladmin/
- SQLAdmin Authentication: https://aminalaee.dev/sqladmin/authentication/

**Существующий код для изучения:**
- Модели БД: `app/infrastructure/database/models/`
- **Текущая аутентификация (ИЗУЧИ В ПЕРВУЮ ОЧЕРЕДЬ):**
  - `app/core/services/auth_service.py` - основная логика JWT
  - `app/api/v1/routers/admin/auth_router.py` - текущие роуты
  - `app/api/v1/dependencies.py` - `get_current_admin_dependency`
  - `app/infrastructure/database/models/admin.py` - модель Admin
- ImageService: `app/core/services/image_service.py`

---

## 7. Ожидаемый результат

После выполнения ТЗ должна быть:
- ✅ Полностью рабочая админ-панель по адресу `/admin`
- ✅ Возможность управлять всеми сущностями
- ✅ Русский интерфейс
- ✅ Защита через JWT аутентификацию
- ✅ Удобное редактирование связанных данных (inline)
- ✅ Загрузка и отображение изображений
- ✅ Seed data для CharacteristicType и Settings

**Примерное время:** 8-12 часов работы

Удачи! 🚀

