# 🔄 Инструкция по перезапуску приложения

## 🚀 Быстрый перезапуск

### Локальная разработка

```bash
# Остановить приложение (Ctrl+C в терминале где запущен uvicorn)
# Затем запустить заново:
uvicorn app.main:app --reload
```

### Docker

```bash
# Перезапустить контейнеры
docker-compose restart

# Или пересобрать и запустить заново
docker-compose down
docker-compose up -d --build
```

---

## 📝 После изменения кода

### Если изменили Python код:

```bash
# При использовании --reload перезапуск автоматический
# Если без --reload, нужно перезапустить вручную:
# Ctrl+C и затем:
uvicorn app.main:app --reload
```

### Если изменили docker-compose.yml:

```bash
docker-compose down
docker-compose up -d
```

### Если изменили Dockerfile:

```bash
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 🔍 Проверка статуса

### Локально

```bash
# Проверить что приложение запущено
curl http://localhost:8000/api/v1/product/home?limit=1

# Проверить админ-панель
curl -I http://localhost:8000/admin
```

### Docker

```bash
# Проверить статус контейнеров
docker-compose ps

# Посмотреть логи
docker-compose logs -f app

# Проверить API
curl http://localhost:8000/api/v1/product/home?limit=1
```

---

## 🐛 Если что-то не работает

### Проблема: Порт занят

```bash
# Найти процесс на порту 8000
lsof -ti:8000

# Убить процесс
kill -9 $(lsof -ti:8000)

# Или использовать другой порт
uvicorn app.main:app --reload --port 8001
```

### Проблема: Изменения не применяются

```bash
# Очистить кеш Python
find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null

# Перезапустить
uvicorn app.main:app --reload
```

### Проблема: Docker контейнеры не стартуют

```bash
# Посмотреть логи
docker-compose logs

# Полная очистка и перезапуск
docker-compose down -v
docker-compose up -d --build
```

---

## ✅ Готово!

После перезапуска проверьте:
- API: http://localhost:8000/docs
- Админ-панель: http://localhost:8000/admin

